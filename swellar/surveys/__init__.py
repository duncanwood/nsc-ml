"""Per-survey adapters: map a survey's catalog onto the canonical detector
frame (see ``swellar.schema``). ``nsc`` is the reference adapter (magnitudes);
``lsst`` maps Rubin ForcedSource/DiaSource flux tables (flux-space, see
PORTABILITY.md)."""
