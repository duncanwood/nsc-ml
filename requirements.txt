# Runtime dependencies for swellar, pinned to the environment the microlensing
# detection pipeline was developed and validated against: conda env "nsc",
# CPython 3.11.9 (the committed __pycache__ is cpython-311). See MAP.md.
#
# pyarrow and tqdm are advanced past that original env to their security-patched
# releases (see AUDIT.md). The full suite was re-run against these pins on
# 2026-08-19: 100 passed on arm64/Darwin, parity goldens still bit-exact.
numpy==1.23.5
pandas==2.2.2
scipy==1.11.4
numba==0.59.1
pyarrow==23.0.1      # pandas parquet I/O backend (read_parquet/to_parquet); CVE-2026-25087, was 16.1.0
tqdm==4.66.3         # CVE-2024-34062, was 4.66.2
matplotlib==3.8.4    # plot.py only; not needed by the detection core
# scikit-learn was used only by the (removed) __deprecated.py KDE segmentation;
# it is not a dependency of the detection pipeline. See AUDIT.md.
