"""Per-survey adapters: map a survey's catalog onto the canonical detector
frame (see ``nscml.schema``). ``nsc`` is the reference adapter; ``lsst`` is
planned for Phase 2 (flux-space; see PORTABILITY.md)."""
